@EndUserText.label: 'übung 15: access control'
@MappingRole: true
define role Z16_ACCESS_CONTROL {
    grant
        select
            on
                Z16_Authorizations
                   where (CarrierID) =
    aspect pfcg_auth(S_CARRID, CARRID, ACTVT = '03'); 
}